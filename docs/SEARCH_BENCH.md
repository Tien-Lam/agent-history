# Search Recall Bench

Generated by `cargo test --test recall_bench`. See [`tests/recall_bench.rs`](../tests/recall_bench.rs) for the harness and [`tests/fixtures/bench_recall/queries.json`](../tests/fixtures/bench_recall/queries.json) for the labeled query set.

Corpus: 12 synthetic single-topic Claude-format sessions, 4 messages each.
Query set: 24 labeled queries.

## Overall

| Strategy | Recall@10 | MRR |
|---|---:|---:|
| lexical | 1.000 | 0.958 |
| semantic | 1.000 | 0.835 |
| hybrid | 1.000 | 0.979 |

## By query type

| Strategy | Tag | Recall@10 | MRR |
|---|---|---:|---:|
| lexical | lexical | 1.000 | 1.000 |
| lexical | semantic | 1.000 | 0.917 |
| semantic | lexical | 1.000 | 0.958 |
| semantic | semantic | 1.000 | 0.711 |
| hybrid | lexical | 1.000 | 1.000 |
| hybrid | semantic | 1.000 | 0.958 |

## Methodology notes

- **lexical** is the production Tantivy BM25 index (`SearchIndex::search`).
- **semantic** is a deterministic hashed-bag-of-words IDF cosine ranker baked into the harness — *not* the production fastembed/MiniLM embedder. It exists so the bench runs in CI without network and so the hybrid row has a non-trivial second signal. When ahist-y3o.4.2 wires fastembed into the index, swap `SemanticRanker` for the real embedder and rerun.
- **hybrid** = Reciprocal Rank Fusion of the two lists, k = 60.
- recall@10 = fraction of queries whose target is in the top 10.
- MRR = mean of 1/rank for each query (rank counted from 1; 0 if outside top 10).
- Query tags: `lexical` queries have the target's exact terms; `semantic` queries use paraphrases. The breakdown surfaces where each strategy actually earns its keep.
